import React, { Component } from 'react';

/**
 * The sticky calendar on the left of the screen of radio
 */
export default class Calendar extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <ul className="calendar">
      </ul>
    );
  }
}
